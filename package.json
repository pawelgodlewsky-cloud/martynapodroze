{
  "name": "martynapodroze-wyjazdy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "lint": "eslint worker src tests worker-public/**/*.js assets/cookie-consent.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir .wrangler/build",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "@eslint/js": "^9.33.0",
    "eslint": "^9.33.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.1"
  }
}
